
extends:
  template: secure.yml
  parameters:
    usersteps:

    - script: echo main script!
    - bash: echo bash
    - powershell: echo "main run!"
    - task: CmdLine@2
      displayName: Test - Will be stripped out
      inputs:
        script: echo This step input!
