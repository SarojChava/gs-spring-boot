name: $(TeamProject)_$(Build.DefinitionName)_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r)
trigger:
- master

variables:
- group: pathargumets
- name: justrefer
  value: $[variables.pathstring]

jobs:
   - job: A
     steps:
      - powershell: |
           Write-Host "Hello"
           Write-Host "path db - $(pathdb)"
           
      - bash: |
           echo "Hello Bash"
           echo "path pass - $(pathpass)"
           echo "addressing all - $(justrefer)"
