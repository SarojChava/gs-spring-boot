extends:
  template: secure.yml
  parameters:
    usersteps:
    
    - task: CmdLine@2
      displayName: Test - Will be stripped out from main
      inputs:
        script: main run
    - bash: echo this is bash -main
    - powershell: echo "This is powershellmain"