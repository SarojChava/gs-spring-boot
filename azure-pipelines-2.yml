trigger:
- master

variables:
- group: pathargumets
- name: justrefer
  value: $[variables.pathstring]

jobs:
   - job: A
     steps:
      - powershell: |
           Write-Host "Hello"
           Write-Host "$(pathargumets.pathdb)"
           
      - bash: |
           echo "Hello Bash"
           echo "$(pathargumets.pathpass)"
           echo "$(justrefer)"
        