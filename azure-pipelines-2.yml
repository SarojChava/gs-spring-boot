
trigger:
- master
variables:
  myAPIname: 'v001/path/account'

jobs:
  - job: A
    steps:
      - powershell: |
          Write-Host "Hello"
          Write-Host "##vso[task.setvariable variable=myapi;isoutput=true]this is path api"
          name: outvar
      - bash: |
          echo " accessing name inside the job - $(outvar)"
          echo "accesing var inside the job - $(myapi)"
  - job: B
    dependsOn: A
    steps:
      - bash: |
          echo "Hello from B"
          echo  " accesing from the other job depends On to code  -$[ dependencies.A.outpus['outvar.myapi']]"